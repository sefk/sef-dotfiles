#!/bin/sh
# Maintain a stable symlink for SSH agent forwarding across tmux sessions
if [ -n "$SSH_AUTH_SOCK" ] && [ "$SSH_AUTH_SOCK" != "$HOME/.ssh/auth_sock" ]; then
    ln -sf "$SSH_AUTH_SOCK" "$HOME/.ssh/auth_sock"
fi
